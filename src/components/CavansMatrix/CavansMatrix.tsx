import classes from "./CavansMatrix.module.css";
import { IProps } from "./CavansMatrix.utils";

const CavansMatrix = (props: IProps) => {
  const {} = props;
  return <section className={classes.rootContainer}>CavansMatrix</section>;
};

export default CavansMatrix;
